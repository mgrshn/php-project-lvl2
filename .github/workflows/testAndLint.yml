name: testAndLint

on: push
  
jobs:
  make-lint-and-tests:
      runs-on: ubuntu-latest

      steps:
        - uses: actions/checkout@v2

        - name: Run lint
          run:  make lint

        - name: run test
          run:  make test